[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

# Handle React Router / SPA routing (prevents 404 on page refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
