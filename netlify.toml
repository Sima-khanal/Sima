[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing for React Router (prevents page refresh 404s)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
